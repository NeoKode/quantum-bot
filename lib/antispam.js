const usedCommandRecently=new Set;export const isFiltered=user=>!!usedCommandRecently.has(user);export const addFilter=user=>{usedCommandRecently.add(user),setTimeout((()=>usedCommandRecently.delete(user)),5e3)}; /** @NeKosmic **/
